// Archivo de configuración con la URL base de la API backend
// Ajústalo si tu backend corre en otra URL o puerto.
// El backend lo arrancaremos en 3001 para evitar conflicto con el dev server del frontend (3000).
// Si prefieres usar otro puerto, actualiza aquí o define PORT al arrancar el backend.
export const API_URL = 'http://localhost:3001/api';
